Plan:

1. Fix the hero “Book an appointment” CTA so its normal/default state is visible without hover.
2. Remove the current conflicting button styling where `outline` still applies dark text over a dark background.
3. Apply a dedicated hero CTA style using semantic theme colors: visible cream/white text, clear cream border, subtle translucent dark/blur background, and stronger shadow.
4. Keep the destination and backend/admin integration unchanged; this is frontend-only.
5. Verify in the live preview that the button is readable in the default hero state, not only on hover.